// Angular 21 mit zoneless Change Detection benötigt zone.js NICHT mehr.
// Diese Datei bleibt leer – zone.js wird weder importiert noch geladen.
// Der build referenziert diese Datei nicht mehr (polyfills: [] in angular.json).
export {};
